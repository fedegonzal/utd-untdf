      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {% for item in menu.items %}          
          <li class="nav-item">
            <a class="nav-link {{ item.classes|join(' ') }}" aria-current="page" href="{{ item.link }}">{{ item.title }}</a>
          </li>
        {% endfor %}
      </ul>
